language: php
php: 5.4

before_script: phpize && ./configure && make && sudo make install
env: REPORT_EXIT_STATUS=1 TEST_PHP_EXECUTABLE=/usr/local/bin/php
script: php run-tests.php -dextension=scalar_objects.so
